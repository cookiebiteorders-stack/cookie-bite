---
description: بعد تعديل الكود — اسأل المستخدم قبل أي commit أو push إلى GitHub
alwaysApply: true
---

# مزامنة GitHub بعد التغييرات

عند إكمال **تعديلات برمجية** في هذا المشروع (ملفات مصدرية، تبعيات، إعدادات بناء):

1. إن غيّرت **`supabase/migrations/`** أو schema — شغّل **`npm run supabase:migrate`** أولاً (انظر قاعدة `supabase-migrate.mdc`).
2. شغّل **`npm run type-check`** (أو **`npm run lint`**) إن كان التغيير قد يكسر الأنواع أو اللينت، وصحّح الأخطاء إن ظهرت.
3. **لا تنفّذ `npm run deploy:github` ولا أي `git commit` / `git push` تلقائياً.**
4. **اسأل المستخدم صراحةً في كل مرة** قبل النشر، مثلاً:
   - «هل تريد دفع التغييرات إلى GitHub الآن؟»
   - اعرض ملخصاً قصيراً لما سيتم رفعه (الملفات أو نوع التغيير).
5. **انتظر موافقة صريحة** (مثل: «نعم»، «ادفع»، «deploy») قبل تشغيل:
   - `npm run deploy:github -- "feat: وصف قصير"`
   - أو أي أمر git للدفع.
6. إن رفض أو لم يرد، **لا تدفع** — اكتفِ بإبلاغه أن التغييرات جاهزة محلياً.
7. إن فشل الـ push (صلاحيات، شبكة)، اذكر للمستخدم تشغيل الأمر يدوياً بعد ضبط Git.

**لا تدفع أبداً** إذا طلب المستخدم **مراجعة فقط** أو **عدم لمس git** صراحةً.

السكربت: `scripts/git-sync.mjs` — يحترم `.gitignore` (مثل `.env`).
